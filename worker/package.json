{
  "name": "kr-control-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute kr-control-db --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute kr-control-db --remote --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "jose": "^5.9.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
